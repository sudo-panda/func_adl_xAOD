[pytest]
addopts = --ignore=setup.py --cov=func_adl_xAOD --cov-report=term-missing --cov-config=.coveragerc --cov-report xml
junit_family=legacy
markers = 
  xaod_runner: Run tests that use docker to do the C++ compile and short runs
